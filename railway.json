{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt && python manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "gunicorn crwn.wsgi:application --bind 0.0.0.0:$PORT --workers 4",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}